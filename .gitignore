# dependencies
node_modules
/.pnp
.pnp.js
.yarn/install-state.gz

# next.js — commit production build for cPanel (Masar cannot npm run build)
# Exclude only the webpack cache (large, not required at runtime)
.next/cache
out

# production
build
dist

# Local CMS restore snapshots (never commit live dumps)
data/cms-store-before-restore-*.json

# environment
.env.local
.env*.local

# logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# misc
.DS_Store
*.pem

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# testing
/coverage

.vercel

.vercel
